class BRE(object):
    def __init(self, args):
        self.num=num
        self.probs=probs #prior probability
        self.terminal=terminal #for stopping
        self.delta=delt #fixed confidence , used for stopping rule
    def sample_selection():
        
    def main():
        terminal=False
        t=0 #time step
        c= 2*ln(2/delta))/delta**2
        while terminal==False:
            t=t+1
            OptSet={}   
            while len(Optset.items)<2 and num<c:
